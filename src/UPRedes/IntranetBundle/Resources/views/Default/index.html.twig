{% extends '::frontend.html.twig' %}
{% block content %}

    {% embed 'UPRedesIntranetBundle:Default:heroNews.html.twig' with {'nupr': nupr} %}
    {% endembed %}
    {% for category in categories %}
        {% if loop.index > 1 %}
        <div class="row-fluid">
            <div class="span12" id="{{ category.slug }}">
                {% render "UPRedesIntranetBundle:Category:show" with { 'id': category.id  } %}
            </div>
        </div>
        <hr>
        {% endif %}
    {% endfor %}
{% endblock %}