<ul class="{{ ul_class }}">
{% for entity in entities %}
    {% if entity.name != 'Destacados' %}
    <li class="{{ li_class }}">
        <a href="#{{ entity.name }}">{{ entity.name }}</a>
    </li>
    {% endif %}
{% endfor %}
</ul>
